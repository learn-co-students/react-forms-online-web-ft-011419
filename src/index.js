import React from 'react';
import ReactDOM from 'react-dom';
import ControlledForm from './components/ControlledInput'

ReactDOM.render(
  <div><ControlledForm /></div>,
  document.getElementById('root')
);